#include "main.h"
/**
 * _isupper -  function that checks for uppercase character.
 * @c: integer to be checked
 *
 * Return: returns 1 if c is uppercas, else 0
 */
int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
